# Borrowed Items App

This folder contains both the backend server and the React client.

## Quick Start

Run `start.sh` on Unix/macOS or `start.bat` on Windows to start the entire application. These scripts install any missing dependencies, launch the server and client, and open the app in your default browser.

Alternatively, you can run the server and client separately as described in their respective READMEs.
