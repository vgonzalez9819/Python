# Borrowed Items Application

This folder contains the backend server and React client.

Run both services at once with:

```bash
node start.js
```

The script installs dependencies if missing, starts the server on port 3001
and the client on port 3000, then opens the browser.

